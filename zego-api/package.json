{
  "name": "zego-api",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "db:migrate:local": "wrangler d1 migrations apply zego-db --local",
    "db:migrate:remote": "wrangler d1 migrations apply zego-db --remote"
  },
  "dependencies": {
    "graphql": "^16.9.0",
    "graphql-yoga": "^5.10.4"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250109.0",
    "typescript": "^5.7.3",
    "wrangler": "^3.99.0"
  }
}
