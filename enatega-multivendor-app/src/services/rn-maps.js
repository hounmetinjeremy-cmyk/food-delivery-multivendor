// Native (iOS/Android) passthrough — see rn-maps.web.js for why this local
// indirection exists. Native behavior is unchanged.
export {
  default,
  Marker,
  Polyline,
  Polygon,
  PROVIDER_DEFAULT,
  PROVIDER_GOOGLE
} from 'react-native-maps'
